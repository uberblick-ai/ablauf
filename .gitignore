node_modules/
dist/
out/

# Agent runtime — the skills (.claude/skills/) and adapters are versioned;
# these are per-machine state the roles and sessions create.
.claude/worktrees/
.claude/settings.local.json
.worktrees/
